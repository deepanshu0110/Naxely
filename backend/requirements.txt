# Web Framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Data Processing
pandas==2.2.0
numpy==1.26.4
openpyxl==3.1.2

# Visualisation (PDF charts only — not served to frontend)
matplotlib==3.8.4
seaborn==0.13.2
squarify==0.4.4
pillow==12.2.0

# PDF + PPT Generation
reportlab==4.2.0
python-pptx==0.6.23

# AI Integrations (user provides their own key)
openai==1.30.0
anthropic==0.26.0

# Google Sheets Connector
google-api-python-client==2.197.0
google-auth==2.55.0

# Security & Encryption
cryptography==48.0.1
python-jose[cryptography]==3.4.0  # JWT validation
slowapi==0.1.9                     # Rate limiting

# Database
sqlalchemy==2.0.30
asyncpg==0.29.0                    # PostgreSQL ASYNC driver (used by FastAPI at runtime)
psycopg2-binary==2.9.9             # PostgreSQL SYNC driver (used by Alembic for migrations ONLY)
alembic==1.13.1

# Supabase Client
# NOTE: supabase==2.9.1 required — 2.4.0 conflicts with httpx>=0.26
# storage3 0.8.2 has a known UnboundLocalError at _sync/file_api.py:50
# when self._client.request() itself raises. Patched in-vendor in venv.
supabase==2.9.1

# Standard Webhooks (Dodo Payments uses this format)
standardwebhooks==1.0.1

# Dodo Payments SDK
dodopayments==1.105.0

# HTTP & File Handling
httpx==0.27.2
python-multipart==0.0.31
aiofiles==23.2.1                   # Async file I/O

# Config & Validation
python-dotenv==1.2.2
pydantic==2.7.1
pydantic-settings==2.3.0

# Scheduling (Agency tier scheduled reports only — NOT used for report generation)
# Report generation uses FastAPI BackgroundTasks instead
apscheduler==3.10.4

# Email
resend==2.0.0

# Monitoring
sentry-sdk[fastapi]==2.8.0

# Testing & Type Checking
pytest==9.1.0
pytest-asyncio==1.4.0
mypy==1.10.0
PyMuPDF==1.27.2.3            # PDF text extraction in tests
pikepdf==10.9.0              # Post-process PDFs (strip unused standard fonts from Resources)